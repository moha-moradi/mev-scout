chain = "arbitrum"
rpc_urls = [
  "https://arb-mainnet.g.alchemy.com/v2/booRpg6FM7gc9g8GC0SdN", # dantemoradi
]

# Per-provider RPS limits (matches rpc_urls order).
# Alchemy: 20 RPS.
rpc_rps = [20]

# Global default RPS (used when rpc_rps is not set for a provider). 0 = unlimited.
rps_limit = 2
