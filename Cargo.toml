[workspace]
resolver = "2"
members = ["core", "cli"]

[workspace.lints.rust]
warnings = "deny"

[workspace.lints.clippy]
unwrap_used = "allow"  # ratchet: ~100 prod sites; CI still denies other warnings
expect_used = "allow"  # ratchet: scoped #[allow] preferred when flipping to deny

[workspace.dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
tokio = { version = "1", features = [
    "rt-multi-thread",
    "macros",
    "sync",
    "time",
] }
alloy = { version = "2", features = [
    "providers",
    "rpc-types",
    "transport-http",
    "rpc-client",
    "consensus",
    "reqwest-default-tls",
    "signers",
    "eips",
    "serde",
] }
rusqlite = { version = "0.31", features = ["bundled"] }

