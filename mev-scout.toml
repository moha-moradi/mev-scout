dune_api_key = "OKfH0tGoc9OVYWOXIaE7yr2jpbEOsEy0"

chain = "polygon"
rpc_urls = [
  "https://shared.ap-southeast-1.getblock.io/0f8be36949d749a59ee2f8d263ec8600",
  "https://polygon-public.nodies.app",
  "https://polygon-bor-rpc.publicnode.com",
  "https://polygon.drpc.org",
  "https://polygon.publicnode.com",
  "https://tenderly.rpc.polygon.community",
]

# Per-provider RPS limits (matches rpc_urls order). Also used as weight for block distribution.
# GetBlock shared: up to 20 RPS. Public endpoints: conservative 10 RPS to avoid 429s.
rpc_rps = [20, 10, 10, 10, 10, 10]

# Global default RPS (used when rpc_rps is not set for a provider). 0 = unlimited.
rps_limit = 0.0

